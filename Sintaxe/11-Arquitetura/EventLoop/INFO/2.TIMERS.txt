╔══════════════════════════════════════════════════════════════════╗
║                Event Loop — Fase Timers                          ║
╚══════════════════════════════════════════════════════════════════╝


┌─ Visão geral ────────────────────────────────────────────────────┐
│                                                                   │
│  A fase Timers é a primeira fase do Event Loop.                   │
│                                                                   │
│  Nela o Node.js executa os callbacks de:                          │
│    • setTimeout()                                                 │
│    • setInterval()                                                │
│                                                                   │
│  O que acontece nessa fase:                                       │
│    1. O loop verifica se algum timer já venceu o delay            │
│    2. Se venceu, executa o callback                               │
│    3. Se não venceu, segue para a próxima fase                    │
│                                                                   │
│  Importante:                                                      │
│    • Timers NÃO usam Thread Pool                                  │
│    • São gerenciados pela libuv, dentro do próprio Event Loop     │
│    • O delay é o tempo mínimo — o callback só roda quando         │
│      o loop chega de novo nessa fase                              │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘


setTimeout(..., 100)
             │
             ▼
       timer registrado
             │
             │ 100ms
             ▼
       timer elegível
             │
             │
             │ Call Stack ocupada?
             │
          SIM│
             ▼
       fica aguardando
             │
             ▼
     Call Stack libera
             │
             ▼
      Event Loop processa


E aí entra a parte interessante:

  O fato de A ter sido registrado primeiro NÃO significa automaticamente que A será executado primeiro em qualquer situação.
  O Node mantém os timers organizados pelo tempo de expiração.


0ms
│
├── A registrado → vence em 1000ms
│
├── B registrado → vence em 100ms
│
│
│   CALL STACK OCUPADA
│   ┌──────────────────────┐
│   │       for (...)      │
│   │                      │
│   │   processamento      │
│   │      pesado          │
│   └──────────────────────┘
│
├── 100ms
│    B já venceu
│
├── 1000ms
│    A também venceu
│
├── 2000ms
│    for termina
│
└── Event Loop pode continuar
